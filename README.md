# Menubot

Menubot makes WordPress admin menu navigation faster for power users.

## Release

Build the release ZIP with:

```bash
scripts/build-plugin-zip.sh
```

The ZIP is written to `dist/menubot.zip` and contains the `menubot/` plugin folder at the top level.
